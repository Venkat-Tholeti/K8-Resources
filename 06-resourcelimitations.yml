apiVersion: v1
kind: Pod
metadata:
   name: resources_limitations
spec:
  containers:
    - name: nginx
      image: nginx
      resources:
        requests: #minimum
         memory: "68Mi"
         cpu: "100m"
        limits: #maximum
          memory: "128Mi"
          cpu: "150m"