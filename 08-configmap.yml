apiVersion: v1
kind: ConfigMap
metadata:
  name: pod-config
  labels:
    purpose: config
    env: dev
data:
  COURSE: "DevOps with AWS"
  DURATION: "120HRS"
  TRAINER: "SIVAKUMAR REDDY M"
  # FOR THIS APPLY BELOW COMMAND TO CHECK AFTER POD CREATION AS THIS WONT SHOW UNDER PODS
  #kubectl get configmap
